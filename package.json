{
  "name": "@cihatsarsilmaz/citv-slot",
  "version": "1.2.0",
  "private": false,
  "type": "module",
  "description": "CITV Slot — web salon, 18 karakter, CITV coin. Store yok. AstrogameWAR değil.",
  "author": "cihatsarsilmaz",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cihatsarsilmaz/citvapp.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sim": "node src/sim.js",
    "lint": "echo 'Lint configured'",
    "test": "echo 'Tests configured'",
    "deploy:web": "npm run build",
    "deploy:vercel": "npm run build && npx vercel --prod",
    "deploy:netlify": "npm run build && npx netlify-cli deploy --prod --dir=dist",
    "deploy:gh": "npm run build && npx gh-pages -d dist"
  },
  "keywords": ["citv-slot", "citv-coin", "slot", "react", "vite", "web"],
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
